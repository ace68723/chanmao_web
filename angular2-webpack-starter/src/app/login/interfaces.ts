export interface User {
    username: string;
    password: string;
}

export interface Option {
  loginMain : boolean;
  signup : boolean;
  forgetPassword : boolean;
  signupSuccess : boolean;
  newPasswordMes: boolean;
  newPassword : boolean;
  passwordMes : boolean;
}
